--Repositorio Git
git add .
git commit -m "Mensaje"
git push origin main
git push --all
git push heroku main

--Cambios en modelos y db
heroku run otree resetdb --app esocial-base
heroku run python manage.py makemigrations
heroku run python manage.py migrate

--Descargar BD postgres
heroku pg:backups:capture --app esocial-base
heroku pg:backups:download --app esocial-base

--Importar BD postgres en local
pg_restore --verbose --clean --no-acl --no-owner -h localhost -U postgres -d testDB latest.dump

--Reporte de una determinada session en una app
SELECT label,_session_code, *
FROM otree_participant
NATURAL JOIN real_effort_numbers_t_t_player --El nombre de la app
WHERE _session_code = '7atzke84'